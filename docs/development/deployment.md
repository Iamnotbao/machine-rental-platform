# Deployment

Deployment targets are planned, not active deployments:

- **Client:** Vercel, Netlify, or GitHub Pages.
- **Admin:** Vercel or Netlify after Phase 1B.
- **Backend:** separate infrastructure.

Each application will remain independently buildable and deployable. Client production output is created with `npm run build:client`.
